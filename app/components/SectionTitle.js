export default function SectionTitle({ children }) {
  return (
    <div style={{
      fontSize: '10px',
      letterSpacing: '3px',
      color: '#1a8c1a',
      textTransform: 'uppercase',
      marginBottom: '12px',
    }}>
      // {children}
    </div>
  );
}
